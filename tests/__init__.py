"""Regression test package marker for release zip self-checks.

The release archive must keep this file so `from tests...` imports used by
the bundled regression suite resolve to the local test helpers.
"""
